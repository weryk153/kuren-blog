import PostWrapper from '../../components/PostWrapper.tsx'

export const meta = {
    title: 'Markdown/MDX with Next.js',
};

I **love** using [Next.js](https://nextjs.org/)

# Post about MDX

export default ({ children }) => <PostWrapper>{children}</PostWrapper>
